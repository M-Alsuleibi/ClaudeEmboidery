# Thread catalogs

Step 3 (thread-match) snaps each quantized color to the nearest real cone here.

Drop catalog CSVs in this folder, one per chart, named to match the
`--thread-chart` value:

- `madeira-polyneon.csv`
- `isacord.csv`

Suggested columns: `code,name,r,g,b`

These are not yet committed — add them before implementing step 3.
