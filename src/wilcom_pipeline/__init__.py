"""Phase A pipeline: photo -> VP3 + worksheet + preview for Wilcom EmbroideryStudio.

The encrypted .emb write happens later, inside a licensed Wilcom install (Phase B).
This package is everything that runs headless on Linux.
"""

__version__ = "0.1.0"
